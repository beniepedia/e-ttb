import React from "react";
import Layout from "@/Layouts/Main";

const PromotionIndex = () => {
    return <div>PromotionIndex</div>;
};

PromotionIndex.layout = (page) => <Layout children={page} />;

export default PromotionIndex;
